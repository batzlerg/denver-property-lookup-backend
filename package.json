{
  "name": "denver-property-lookup-backend",
  "version": "1.0.0",
  "author": "",
  "main": "index.ts",
  "description": "Backend for Denver Property Lookup",
  "license": "ISC",
  "scripts": {
    "postinstall": "cd web && npm install",
    "start": "docker compose up -d --build web",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}